<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="saac.Views.PublicacaoSelecionadaPage"
             Title="Comentarios">

    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Nome}"/>
            <Label Text="{Binding Publication.Texto}"/>
            <Entry Placeholder="Comentário"
                       Text="{Binding Comentarios.Texto}"/>

            <Button Text="Salvar" TextColor="White"
                        BackgroundColor="#77D065"
                        Command="{Binding SalvarComentarioCommand}"/>

            <Label Text="{Binding Message}"/>

            <ListView ItemsSource="{Binding ComentariosPublication}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Texto}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

        </StackLayout>

    </ContentPage.Content>

</ContentPage>